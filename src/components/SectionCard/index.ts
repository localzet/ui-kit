import { SectionCardSection } from './SectionCardSection'
import { SectionCardRoot } from './SectionCardRoot'

export const SectionCard = {
    Root: SectionCardRoot,
    Section: SectionCardSection
}
